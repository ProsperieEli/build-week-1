const raiseExponent = require('./pow.js');

describe('raiseExponent', () => {
  it('takes a base and exponent and raises base to exponent power', () => {
    const exponent = raiseExponent({
            
    });
  });
});
